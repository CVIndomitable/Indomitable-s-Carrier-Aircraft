# 不挠的舰载机模组



现在还在开发，修改的时候不要在意存档兼容性



## 项目信息

- **MC 版本**: 1.21.1
- **加载器**: NeoForge
- 
- **创建日期**: 2026-06-02

> 

- 


- [ ] 

> 

## 技术栈

### 核心依赖
- NeoForge (1.21.1)
- Java 21+

### 可能需要的库
- （待定）JourneyMap API（如果要集成地图）
- （待定）配置库（如果配置复杂）

## 开发规范

### 代码架构
- 所有机型配置使用 JSON 数据包
- 状态机使用接口 + 枚举模式
- 弹道计算封装在独立的工具类
- 网络包使用 NeoForge 的 SimpleChannel
- 客户端渲染使用事件总线

### 包结构
```
com.indomitable.carrieraircraft
├── aircraft/            # 飞机类型定义与数据
├── entity/              # 飞机实体
│   └── ai/              # AI 与状态机
├── ballistic/           # 弹道计算
├── firecontrol/         # 火控系统
├── targeting/           # 目标锁定
├── formation/           # 编组系统
├── client/              # 客户端
│   ├── model/           # 模型
│   ├── renderer/        # 渲染器
│   └── screen/          # 屏幕/GUI
├── item/                # 物品
├── menu/                # 菜单容器
├── network/             # 网络同步
├── event/               # 事件处理
├── config/              # 配置
└── registry/            # 注册
```

### 变更管理
- **所有方案变更必须记录**: 开发过程中涉及架构调整、技术选型变化、功能范围调整等重要决策，都要及时记录到 `docs/方案变更记录.md`
- 记录内容包括：变更日期、类型、原因、影响范围、决策者
- 保持记录简洁，详细内容可链接到其他文档

## 需求

### 核心目标

实现一个 Minecraft 模组，让玩家可以召唤并指挥 AI 驾驶的舰载机编队执行对海/对空/反潜作战。

### 功能需求

1. **机型系统**：双武器槽设计，支持水平轰炸机、俯冲轰炸机、鱼雷机、火箭机、反潜机等，所有机型参数数据驱动（JSON 数据包），新增机型不改代码
2. **弹药系统**：子弹弹匣（可合并）、航空炸弹、鱼雷、火箭弹；投弹数量与最小有效伤害可配
3. **弹道与命中**：自由落体、制导、自走（鱼雷/火箭）四种弹道类型；三坐标体系（目标坐标→弹着点→投弹坐标）
4. **状态机**：STANDBY → APPROACH → LOCKED → ATTACKING → DROPPING → POST_ATTACK → RETURNING 完整生命周期，含空战（DOGFIGHT）状态；返航直接降落并回收
5. **锁定与火控**：自动锁定档位（最近/最强/集火/分散/类型过滤）+ 玩家手动火控锁定，支持集火/普通分配模式
6. **编组系统**：大队→中队→小队三级编组，等齐出发、任务过滤、对空模式三档（一般/主动/低攻击性）
7. **长机与侦察视角**：长机作为侦察视角锚点和区块加载锚点，玩家可切入第一人称视角，多长机切换
8. **反潜探测**：反潜机飞行时高亮水下敌对目标，玩家/长机视角均可见
9. **控制面板**：手持终端物品，含地图、配置、指挥、弹药管理功能

### 非功能需求

- MC 1.21.1 + NeoForge，Java 21+
- 所有机型配置数据驱动，代码不硬编码数值
- 状态机解耦，动画与逻辑分离
- 支持多人场景（敌我识别、队伍系统）

## 策划方案

### 整体架构

分为四个层次：核心执行层（飞机实体、状态机、弹道）、决策控制层（锁定、火控、编组）、特殊功能层（长机管理、反潜、弹药管理）、界面层（控制面板 GUI）。

依赖关系：控制面板 → 所有系统；火控 → 锁定 → 状态机 → 弹道；编组 → 长机系统。

### 机型设计

| 机型 | 对空槽 | 对海槽允许弹药 | 攻击模式 |
|------|--------|----------------|----------|
| B-25 | 前射 + 自卫炮塔 | 航弹 | 水平轰炸 |
| BTD | 前射 | 航弹 / 鱼雷 | 俯冲轰炸 / 鱼雷 |
| 反潜机 | — | 航弹 | 水平轰炸（目标=水下） |

对空能力类型：前射（主动追击）、自卫炮塔（被动防御球形范围）、两者兼有、无。

### 空袭流程

```
锁定子系统给出目标 → 计算三坐标 → APPROACH 赶赴投弹坐标
→ DROPPING 投弹 → 检查弹药
  ├── 不足 → RETURNING
  └── 充足 → APPROACH 赶赴转向点 → POST_ATTACK 确认效果
              ├── 目标死亡 → 重新索敌
              └── 目标存活 → 重新计算三坐标
```

### 编组与调度

- 大队（混编）：统一调度目的地/盘旋点
- 中队（同攻击类型）：对空/鱼雷/轰炸/反潜
- 小队（同机型）：细化管理单位
- 等齐出发：按所选层级为单位等待齐全再出发
- 任务过滤：按目标类型自动筛选参与中队

### 敌我识别系统

采用多层次识别规则（优先级从高到低）：

1. **实体类型标签**（数据包可自定义）
   - `indomitablecarrieraircraft:friendly_entities` — 友方白名单
   - `indomitablecarrieraircraft:hostile_entities` — 敌对黑名单
2. **所有者识别**：同所有者的召唤物为友方
3. **队伍系统**：同队伍的玩家及其召唤物为友方（支持 `/team` 命令）
4. **默认规则**：`Enemy` 接口为敌对，`Animal`/`Villager` 为友方

详细说明见 `docs/敌我识别系统.md`。

### 待策划细化项

详见 `docs/待策划细化清单.md`，关键待决策包括：
- ~~敌我识别规则（tag 白名单 + 队伍支持）~~ ✅ 已实现
- 弹药经济（合成配方、补给流程）
- 空战手感（弹道投射 vs 即时命中）
- 鱼雷与反潜玩法深化
- 编组系统三级编队具体规则

## 实现方法

### 已实现（Phase 1 MVP）

- **飞机实体**：`AircraftEntity` 基类 + 飞行物理（`FlightPhysics`）+ 参数接口（`AircraftSpec`）
- **状态机**：`IPlaneState` 接口 + 枚举 `AircraftState`，挂载在 `aiStep()` 中
- **弹道系统**：`ballistic/` 包下 `BombTrajectory`、`TorpedoTrajectory` 等，三坐标计算封装在 `FireControlSystem`
- **火控**：`FireControlSystem` 管理目标列表与坐标计算，支持手动/自动锁定
- **敌我识别**：`FriendlyFireFilter` 多层次识别（标签 > 所有者 > 队伍 > 默认），数据包可自定义
- **数据驱动机型**：`AircraftSpecLoader` 从 JSON 数据包加载机型配置，支持热重载（`/reload`）
- **物品与注册**：`AircraftSpawnerItem` 召唤飞机，`ModEntityTypes`/`ModItems`/`ModBlocks` 注册
- **渲染**：`BomberAircraftRenderer`、`BombEntityRenderer` 客户端渲染

### 技术选型

| 方面 | 实现方式 |
|------|----------|
| 状态机 | 接口 + 枚举，`aiStep()` 驱动 |
| 弹道计算 | 独立工具类封装，按弹种区分 |
| 网络同步 | NeoForge SimpleChannel |
| 客户端渲染 | 事件总线注册渲染器 |
| 机型数据 | JSON 数据包（`data/<namespace>/aircraft/<id>.json`），由 `AircraftSpecLoader` 加载 |

### 包结构

```
com.indomitable.carrieraircraft
├── aircraft/            # 飞机类型定义与数据
├── entity/              # 飞机实体
│   └── ai/              # AI 与状态机
├── ballistic/           # 弹道计算
├── firecontrol/         # 火控系统
├── client/              # 客户端渲染
├── item/                # 物品
├── event/               # 事件处理
└── registry/            # 注册
```

### 开发路线

- **Phase 1（已完成）**：MVP 水平轰炸机，实体/渲染/物品/状态机/火控
- **Phase 2（进行中）**：数据驱动机型、编组系统、多机型支持、合成配方
- **Phase 3（规划中）**：控制面板 GUI、长机视角、区块加载、反潜系统

### 变更管理

- 所有方案变更记录到 `docs/方案变更记录.md`
- 记录内容：变更日期、类型、原因、影响范围

## 参考资料

- [NeoForge 文档](https://docs.neoforged.net/)
- Minecraft 原版飞行实体（Phantom、Vex）
- 皮兰港项目经验（火控高亮、实体同步等）
- `docs/不挠的舰载机原始方案.md` — 完整策划案
- `docs/不挠的舰载机-子系统划分.md` — 子系统详细设计

---

